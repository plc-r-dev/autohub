/**
 * Shared Admin layout shell.
 * @deprecated Prefer importing `AdminShell` directly from `@/components/admin/shell/admin-shell`.
 * Kept as a stable alias so existing pages can migrate incrementally.
 */
export { AdminShell as AdminLayout } from "@/components/admin/shell/admin-shell"
